{
  "name": "@devprep/execution-service",
  "version": "1.0.0",
  "private": true,
  "description": "Isolated code execution service — the only component allowed to run untrusted code",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "images:build": "docker build -t devprep-runner-node:1 runners/node && docker build -t devprep-runner-python:1 runners/python"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
